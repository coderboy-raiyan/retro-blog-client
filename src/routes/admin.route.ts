import { TRoute } from "@/types";

const adminRoutes: TRoute[] = [
  {
    title: "Admin - Management",
    items: [
      {
        title: "Analytics",
        url: "/analytics",
      },
    ],
  },
];

export default adminRoutes;
