import { TRoute } from "@/types";

const userRoutes: TRoute[] = [
  {
    title: "Blog Management",
    items: [
      {
        title: "Create Blog",
        url: "/dashboard",
      },
    ],
  },
];

export default userRoutes;
